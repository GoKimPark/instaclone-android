plugins {
    id 'org.jetbrains.kotlin.jvm'
}

version 'unspecified'

dependencies {
    implementation project(":utility")
    compileClasspath "javax.inject:javax.inject:1"
}

test {
    useJUnitPlatform()
}